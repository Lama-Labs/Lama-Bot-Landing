'use client'
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#664DFF',
    },
    secondary: {
      main: '#F7931A',
    },
  },
})

export default theme
